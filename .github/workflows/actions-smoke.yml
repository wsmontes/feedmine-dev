name: Actions Smoke Test

on:
  push:
    branches:
      - feat/feed-publication-stability-20260904
    paths:
      - '.github/workflows/actions-smoke.yml'

jobs:
  smoke:
    runs-on: ubuntu-latest
    steps:
      - name: Runner smoke test
        run: echo "runner-ok"
